<resources>
    <!-- Base application theme. -->
    <style name="Base.Theme.ConverterApp" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
        <item name="android:fontFamily">@font/baloo2</item>
    </style>

    <style name="Theme.ConverterApp" parent="Base.Theme.ConverterApp" />
</resources>
